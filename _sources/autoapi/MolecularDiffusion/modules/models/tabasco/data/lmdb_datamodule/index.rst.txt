MolecularDiffusion.modules.models.tabasco.data.lmdb_datamodule
==============================================================

.. py:module:: MolecularDiffusion.modules.models.tabasco.data.lmdb_datamodule


Attributes
----------

.. autoapisummary::

   MolecularDiffusion.modules.models.tabasco.data.lmdb_datamodule.log


Classes
-------

.. autoapisummary::

   MolecularDiffusion.modules.models.tabasco.data.lmdb_datamodule.LmdbDataModule


Module Contents
---------------

.. py:class:: LmdbDataModule(data_dir: str, lmdb_dir: str, add_random_rotation: bool = False, add_random_permutation: bool = False, reorder_to_smiles_order: bool = False, remove_hydrogens: bool = True, batch_size: int = 256, num_workers: int = 0, val_data_dir: Optional[str] = None, test_data_dir: Optional[str] = None)

   Bases: :py:obj:`lightning.LightningDataModule`


   PyTorch Lightning `DataModule` for unconditional ligand generation.


   .. py:method:: get_dataset_stats()

      Return statistics dictionary computed by the training dataset.



   .. py:method:: prepare_data()

      Create LMDB files if they are missing (handled lazily by dataset).



   .. py:method:: setup(stage: Optional[str] = None)

      Instantiate train/val/test datasets.

      If val_data_dir is None, the training file is randomly split into
      train and validation indices. Otherwise the provided paths are used.



   .. py:method:: test_dataloader()

      Return the test `DataLoader` (falls back to validation set when absent).



   .. py:method:: train_dataloader()

      Return the training `DataLoader`.



   .. py:method:: val_dataloader()

      Return the validation `DataLoader`.



   .. py:attribute:: batch_size
      :value: 256



   .. py:attribute:: data_dir


   .. py:attribute:: dataset_kwargs

      Args:
      data_dir: Path to the training set .pt file produced by preprocessing.
      lmdb_dir: Directory where LMDB files and stats are stored.
      add_random_rotation: Apply random rotations inside each dataset item.
      add_random_permutation: Randomly permute heavy-atom order in each item.
      reorder_to_smiles_order: Re-index atoms to canonical SMILES before tensorization.
      remove_hydrogens: Strip explicit hydrogens before conversion to tensors.
      batch_size: Number of molecules per batch.
      num_workers: DataLoader worker count.
      val_data_dir: Optional path to a separate validation set; if None, a train/val split is created.
      test_data_dir: Optional path to a held-out test set.


   .. py:attribute:: lmdb_dir


   .. py:attribute:: num_workers
      :value: 0



   .. py:attribute:: test_data_dir
      :value: None



   .. py:attribute:: val_data_dir
      :value: None



.. py:data:: log

