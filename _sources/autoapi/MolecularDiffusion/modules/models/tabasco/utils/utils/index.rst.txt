MolecularDiffusion.modules.models.tabasco.utils.utils
=====================================================

.. py:module:: MolecularDiffusion.modules.models.tabasco.utils.utils


Attributes
----------

.. autoapisummary::

   MolecularDiffusion.modules.models.tabasco.utils.utils.log


Functions
---------

.. autoapisummary::

   MolecularDiffusion.modules.models.tabasco.utils.utils.extras
   MolecularDiffusion.modules.models.tabasco.utils.utils.get_metric_value
   MolecularDiffusion.modules.models.tabasco.utils.utils.log_hyperparameters
   MolecularDiffusion.modules.models.tabasco.utils.utils.task_wrapper


Module Contents
---------------

.. py:function:: extras(cfg: omegaconf.DictConfig) -> None

   Applies optional utilities before the task is started.

   Utilities:
       - Ignoring python warnings
       - Setting tags from command line
       - Rich config printing

   :param cfg: Hydra `DictConfig` with an optional `extras` section.


.. py:function:: get_metric_value(metric_dict: Dict[str, Any], metric_name: Optional[str]) -> Optional[float]

   Safely retrieves value of the metric logged in LightningModule.

   :param metric_dict: Dictionary of metric names to tensors.
   :param metric_name: Name of the metric to fetch. If `None`, the function
                       returns `None` after logging.

   :returns: Scalar metric value or `None`.
   :rtype: float | None


.. py:function:: log_hyperparameters(object_dict: Dict[str, Any]) -> None

   Controls which config parts are saved by Lightning loggers.

   Additionally saves:
       - Number of model parameters

   :param object_dict: Must contain `cfg`, `model`, and `trainer` entries.

   object_dict: A dictionary containing the following objects:
       - `"cfg"`: A DictConfig object containing the main config.
       - `"model"`: The Lightning model.
       - `"trainer"`: The Lightning trainer.


.. py:function:: task_wrapper(task_func: Callable) -> Callable

   Optional decorator that controls the failure behavior when executing the task function.

   This wrapper can be used to:
       - make sure loggers are closed even if the task function raises an exception (prevents multirun failure)
       - save the exception to a `.log` file
       - mark the run as failed with a dedicated file in the `logs/` folder (so we can find and rerun it later)
       - etc. (adjust depending on your needs)

   Example:
   ```
   @utils.task_wrapper
   def train(cfg: DictConfig) -> Tuple[Dict[str, Any], Dict[str, Any]]:
       ...
       return metric_dict, object_dict
   ```

   :param task_func: Original task function taking a single `cfg` argument.

   :returns: Wrapped task function.
   :rtype: Callable


.. py:data:: log

