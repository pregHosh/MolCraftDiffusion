MolecularDiffusion.modules.models.ldm.encoders.transformer
==========================================================

.. py:module:: MolecularDiffusion.modules.models.ldm.encoders.transformer

.. autoapi-nested-parse::

   Transformer encoder for VAE.

   Copyright (c) Meta Platforms, Inc. and affiliates.
   Adapted for MolecularDiffusion.



Classes
-------

.. autoapisummary::

   MolecularDiffusion.modules.models.ldm.encoders.transformer.TransformerEncoder


Functions
---------

.. autoapisummary::

   MolecularDiffusion.modules.models.ldm.encoders.transformer.get_index_embedding


Module Contents
---------------

.. py:class:: TransformerEncoder(max_num_elements: int = 100, d_model: int = 256, nhead: int = 8, dim_feedforward: int = 1024, activation: str = 'gelu', dropout: float = 0.0, norm_first: bool = True, bias: bool = True, num_layers: int = 4)

   Bases: :py:obj:`torch.nn.Module`


   Transformer encoder as part of standard Transformer-based VAEs.

   :param max_num_elements: Maximum number of elements (atomic numbers) supported
   :param d_model: Dimension of the model
   :param nhead: Number of attention heads
   :param dim_feedforward: Dimension of the feedforward network
   :param activation: Activation function to use
   :param dropout: Dropout rate
   :param norm_first: Whether to use pre-normalization in Transformer blocks
   :param bias: Whether to use bias
   :param num_layers: Number of layers


   .. py:method:: forward(batch) -> Dict[str, torch.Tensor]

      :param batch: PyG Data object with the following attributes:
                    atom_types (torch.Tensor): Atomic numbers of atoms (n,)
                    pos (torch.Tensor): Cartesian coordinates (n, 3)
                    frac_coords (torch.Tensor): Fractional coordinates (n, 3) - zeros for molecules
                    num_atoms (torch.Tensor): Number of atoms per sample (batch_size,)
                    batch (torch.Tensor): Batch index for each atom (n,)
                    token_idx (torch.Tensor): Token index within each molecule (n,)

      :returns: x (n, d), num_atoms, batch, token_idx
      :rtype: Dict with keys



   .. py:attribute:: atom_type_embedder


   .. py:attribute:: d_model
      :value: 256



   .. py:attribute:: frac_coords_embedder


   .. py:attribute:: max_num_elements
      :value: 100



   .. py:attribute:: num_layers
      :value: 4



   .. py:attribute:: pos_embedder


   .. py:attribute:: transformer


.. py:function:: get_index_embedding(indices, emb_dim, max_len=2048)

   Creates sine / cosine positional embeddings from a prespecified indices.

   :param indices: offsets of size [..., num_tokens] of type integer
   :param emb_dim: dimension of the embeddings to create
   :param max_len: maximum length

   :returns: positional embedding of shape [..., num_tokens, emb_dim]


