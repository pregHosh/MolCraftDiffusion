MolecularDiffusion.utils.geom_utils
===================================

.. py:module:: MolecularDiffusion.utils.geom_utils


Functions
---------

.. autoapisummary::

   MolecularDiffusion.utils.geom_utils.assert_correctly_masked
   MolecularDiffusion.utils.geom_utils.assert_mean_zero
   MolecularDiffusion.utils.geom_utils.assert_mean_zero_with_mask
   MolecularDiffusion.utils.geom_utils.check_mask_correct
   MolecularDiffusion.utils.geom_utils.coord2cosine
   MolecularDiffusion.utils.geom_utils.coord2diff
   MolecularDiffusion.utils.geom_utils.correct_edges
   MolecularDiffusion.utils.geom_utils.create_pyg_graph
   MolecularDiffusion.utils.geom_utils.random_rotation
   MolecularDiffusion.utils.geom_utils.read_xyz_file
   MolecularDiffusion.utils.geom_utils.remove_mean
   MolecularDiffusion.utils.geom_utils.remove_mean_with_mask
   MolecularDiffusion.utils.geom_utils.remove_mean_with_mask_v2
   MolecularDiffusion.utils.geom_utils.sample_center_gravity_zero_gaussian_with_mask
   MolecularDiffusion.utils.geom_utils.sample_gaussian_with_mask
   MolecularDiffusion.utils.geom_utils.save_xyz_file
   MolecularDiffusion.utils.geom_utils.save_xyz_file_atomic_numbers
   MolecularDiffusion.utils.geom_utils.translate_to_origine


Module Contents
---------------

.. py:function:: assert_correctly_masked(variable: torch.Tensor, node_mask: torch.Tensor) -> None

   Asserts that the masked values in the variable are close to zero.

   :param variable: Input tensor.
   :type variable: torch.Tensor
   :param node_mask: Boolean mask indicating valid nodes.
   :type node_mask: torch.Tensor


.. py:function:: assert_mean_zero(x: torch.Tensor) -> None

   Asserts that the mean of a tensor along dimension 1 is close to zero.

   :param x: Input tensor.
   :type x: torch.Tensor


.. py:function:: assert_mean_zero_with_mask(x, node_mask, eps=1e-10)

.. py:function:: check_mask_correct(variables: list, node_mask: torch.Tensor) -> None

   Checks if variables are correctly masked using assert_correctly_masked.

   :param variables: List of variables to check.
   :type variables: list
   :param node_mask: Node mask to apply.
   :type node_mask: torch.Tensor


.. py:function:: coord2cosine(x, edge_index, epsilon=1e-08)

.. py:function:: coord2diff(x: torch.Tensor, edge_index: torch.Tensor, norm_constant: float = 1.0) -> Tuple[torch.Tensor, torch.Tensor]

   Calculates the radial distance and normalized coordinate difference between nodes connected by edges.

   :param x: Node coordinates of shape (num_nodes, 3).
   :type x: torch.Tensor
   :param edge_index: Edge indices of shape (2, num_edges).
   :type edge_index: torch.Tensor
   :param norm_constant: Constant added to the normalization term for numerical stability. Defaults to 1.0.
   :type norm_constant: float, optional

   :returns: Radial distances of shape (num_edges, 1) and normalized coordinate differences of shape (num_edges, 3).
   :rtype: Tuple[torch.Tensor, torch.Tensor]


.. py:function:: correct_edges(data, scale_factor=1.3)

   Corrects the edges in a molecular grapSCALE_FACTORh based on covalent radii.
   This function iterates over the nodes and their adjacent nodes in the given
   molecular graph data. It calculates the bond length between each pair of nodes
   and checks if it is within the allowed bond length threshold (sum of covalent radii plus relaxation factor).
   If the bond length is valid, the edge is kept; otherwise, it is removed.

   Parameters:
   data (torch_geometric.data.Data): The input molecular graph data containing node features,
                                     edge indices, and positions.
   scale_factor (float): The scaling factor to apply to the covalent radii. Default is 1.3.

   Returns:
   torch_geometric.data.Data: The corrected molecular graph data with updated edge indices.


.. py:function:: create_pyg_graph(cartesian_coordinates_tensor, atomic_numbers_tensor, xyz_filename=None, r=5.0)

   Creates a PyTorch Geometric graph from given cartesian coordinates and atomic numbers.
   :param cartesian_coordinates_tensor: A tensor containing the cartesian coordinates of the atoms.
   :type cartesian_coordinates_tensor: torch.Tensor
   :param atomic_numbers_tensor: A tensor containing the atomic numbers of the atoms.
   :type atomic_numbers_tensor: torch.Tensor
   :param xyz_filename: The filename of the XYZ file.
   :type xyz_filename: str
   :param r: The radius within which to consider edges between nodes. Default is 5.0.
   :type r: float, optional

   :returns: A PyTorch Geometric Data object containing the graph representation of the molecule.
   :rtype: torch_geometric.data.Data


.. py:function:: random_rotation(x)

.. py:function:: read_xyz_file(xyz_file)

   Reads an XYZ file and extracts atomic positions and atomic numbers.
   :param xyz_file: Path to the XYZ file.
   :type xyz_file: str

   :returns:

             A tuple containing:
                 - cartesian_coordinates_tensor (torch.Tensor): Tensor of shape (N, 3) with the Cartesian coordinates of the atoms.
                 - atomic_numbers_tensor (torch.Tensor): Tensor of shape (N,) with the atomic numbers of the atoms.
   :rtype: tuple


.. py:function:: remove_mean(x: torch.Tensor) -> torch.Tensor

   Removes the mean from a tensor along dimension 1.

   :param x: Input tensor.
   :type x: torch.Tensor

   :returns: Mean-centered tensor.
   :rtype: torch.Tensor


.. py:function:: remove_mean_with_mask(x: torch.Tensor, node_mask: torch.Tensor) -> torch.Tensor

   Removes the mean from a tensor along dimension 1, considering a node mask.

   :param x: Input tensor.
   :type x: torch.Tensor
   :param node_mask: Boolean mask indicating valid nodes.
   :type node_mask: torch.Tensor

   :returns: Mean-centered tensor.
   :rtype: torch.Tensor


.. py:function:: remove_mean_with_mask_v2(pos: torch.Tensor, node_mask: torch.Tensor) -> torch.Tensor

   Removes the mean from a tensor along dimension 1, considering a node mask.

   :param pos: Input tensor of shape (bs, n, 3).
   :type pos: torch.Tensor
   :param node_mask: Boolean mask of shape (bs, n) indicating valid nodes.
   :type node_mask: torch.Tensor

   :returns: Mean-centered tensor.
   :rtype: torch.Tensor


.. py:function:: sample_center_gravity_zero_gaussian_with_mask(size, device, node_mask, std=1.0)

.. py:function:: sample_gaussian_with_mask(size, device, node_mask, std=1.0)

.. py:function:: save_xyz_file(path, one_hot, positions, atom_decoder, id_from=0, name='molecule', node_mask=None, idxs=None, tol=0.0001, atomic_numbers=None, use_unknown_fallback=False)

   Save XYZ files for a batch of molecules, skipping atoms near (0,0,0).

   :param path: Output directory
   :param one_hot: [B, N, C] one-hot encoding
   :param positions: [B, N, 3] coordinates
   :param atom_decoder: List mapping indices to atom symbols
   :param id_from: Starting index for filenames
   :param name: Filename prefix
   :param node_mask: Optional [B, N] or [B, N, 1] mask
   :param idxs: Optional indices for filenames
   :param tol: Tolerance for filtering atoms near origin
   :param atomic_numbers: Optional [B, N] atomic numbers for fallback
   :param use_unknown_fallback: If True and argmax hits unknown column, use atomic_numbers


.. py:function:: save_xyz_file_atomic_numbers(path: str, positions: torch.Tensor, atomic_numbers: torch.Tensor, id_from: int = 0, name: str = 'molecule', node_mask: torch.Tensor | None = None, idxs=None, tol: float = 0.0001)

   Save XYZ files for a batch of molecules, writing ATOMIC SYMBOLS in the first column.

   :param path: output directory
   :param positions: (B, N, 3) tensor
   :param atomic_numbers: (B, N) long tensor
   :param id_from: starting index for filenames
   :param name: filename prefix
   :param node_mask: optional (B, N) mask; if provided, considers first sum(mask) atoms per molecule
   :param idxs: optional iterable of indices (len B) to use in filenames
   :param tol: atoms with coords ~ (0,0,0) are skipped (|coord| <= tol in all dims)


.. py:function:: translate_to_origine(coords, node_mask)

